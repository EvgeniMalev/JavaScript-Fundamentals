function LargestNumber(num1, num2, num3) {
    let tempbignum = LargestNumber(num1, num2);
    let bignum = LargestNumber(num1, num2);

   function LargestNumber(num1, num2) {
       if (num1 < num2) {
          return num1;
       } else {
          return num2;
       }
}

LargestNumber(3, 6, 1);
